# Plano: Correção dos 6 Erros Restantes do `cargo check`

## Situação Atual
- **511 → 6 erros** (corrigidos 505 erros na sessão anterior)
- Erros restantes são problemas estruturais mais complexos

## Erro 1: `paint_layout_tree.rs:160` — `include!()` em `match`
**Causa**: `include!()` não pode expandir para match arms (limitação do parser Rust)
**Arquivo**: `src/renderer/paint/paint_layout_tree.rs` linhas 158-164
**Fix**: Criar `paint_form_controls.rs` com o `match` completo e substituir:
```rust
if prim.element_type == ElementRenderType::Input {
    include!("paint_form_controls.rs");
}
```

## Erro 2: `gpucompositor_impl_1.rs:82` — `shaders.wgsl` faltando
**Causa**: `include_str!("shaders.wgsl")` resolve relativo ao arquivo; o `.wgsl` está em `../shaders.wgsl`
**Arquivo**: `src/ace/engine/graphics/compositor/gpucompositor_impl_1.rs` linha 82
**Fix**: Mudar para `include_str!("../shaders.wgsl")`

## Erro 3: `aceengine.rs:146` — doc comment antes de `include!()` em impl
**Causa**: `/// TODO: add docs` antes de `include!()` — parser não consegue anexar attribute a macro
**Arquivo**: `src/ace/engine/core/aceengine.rs` linha 145-146
**Fix**: Remover o `/// TODO: add docs` da linha 145

## Erro 4: `layout_table.rs:2` — `fn` dentro de `if` block
**Causa**: `pub(crate) fn scan_table_children()` fica dentro do corpo de `else if` após expansão
**Arquivo**: `src/ace/engine/layout_builder/layout_table.rs` linhas 11-88
**Fix**: Mover `scan_table_children` para fora do include (nível de módulo ou method no impl)

## Erro 5: `layout_subgrid.rs:2` — `let` onde expression esperado
**Causa**: Parser trata `include!()` como expression trailing; expansão começa com `let`
**Arquivo**: `src/ace/engine/layout_builder/layout_subgrid.rs`
**Fix**: Envolver em bloco explícito: `{ include!("layout_subgrid.rs"); }`

## Erro 6: `layout_svg.rs:1` — delimiter inesperado `}`
**Causa**: Arquivo começa com `} else if el.tag == "svg" {` que duplica o contexto do include
**Arquivo**: `src/ace/engine/layout_builder/layout_svg.rs` linha 1
**Fix**: Remover a primeira linha `} else if el.tag == "svg" {`

## Arquivos a Modificar
1. `src/renderer/paint/paint_layout_tree.rs`
2. `src/renderer/paint/paint_form_controls.rs` (novo)
3. `src/ace/engine/graphics/compositor/gpucompositor_impl_1.rs`
4. `src/ace/engine/core/aceengine.rs`
5. `src/ace/engine/layout_builder/layout_table.rs`
6. `src/ace/engine/layout_builder/layout_subgrid.rs`
7. `src/ace/engine/layout_builder/layout_svg.rs`

## Verificação
```bash
cargo check 2>&1 | grep "^error" | wc -l
```
Esperado: 0 erros
